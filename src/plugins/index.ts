/**
 * plugins/
 * Drop-in plugin packages live here.
 * Each plugin exports a manifest matching the PluginDefinition interface in core/plugins.
 *
 * Example structure:
 *   plugins/
 *     contact-form/
 *       index.ts      ← re-exports manifest
 *       Component.tsx
 */
export {};
